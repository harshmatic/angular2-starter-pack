import {
  Component,
  Input
} from '@angular/core';
import { FormControl } from '@angular/forms';

@Component({
  selector: 'espl-pageheader',
  template: `
  <div class="page-header">
  <h3>Testing Angular 2 Components <small>(Page header component)</small></h3>
</div>

  `
})
export class EsplPageheaderComponent {
  
};
